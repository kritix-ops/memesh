node_modules/
dist/
build/
.next/
.turbo/
.cache/
coverage/
*.log
*.tsbuildinfo
.DS_Store
Thumbs.db
.vscode/
.idea/
.env
.env.local
.env.*.local
.env.production
.env.preview
.env.development
.pnpm-store/

.vercel
.env*.local

.commit-msg.txt

.vercel

# Rotated-secret stash from Phase 6 cutover. Never commit.
.secrets-rotated.env

# Generated by apps/api-deploy/scripts/build-api-bundle.mjs during build.
apps/api-deploy/lib/api-bundle.mjs

# WordPress plugin source + ZIP. The plugin file historically shipped the
# WP_HANDOFF_SHARED_SECRET inline; it now reads from wp-config.php / env, but
# the working copy + the upload ZIP remain a place where a stray hardcoded
# value could leak. Keep the plugin out of git; ship via the release ZIP only.
wp-plugin/
